# author:insthink
# contact: insthink@qq.com
# datetime:21/8/29 下午12:53
# software: PyCharm

import random

if __name__ == '__main__':
    # scatter
    random.seed(666)
    # x = [random.randint(0, 100) for _ in range(100)]
    # y = [random.randint(0, 100) for _ in range(100)]
    # plt.scatter(x, y)
    # plt.show()
    # plt.plot([i for i in range(100)], y)
    # plt.show()

    # data = [1, 1, 1, 2, 2, 2, 3, 3, 4]
    # counter = Counter(data)
    # x = [p[0] for p in counter.most_common()]
    # y = [p[1] for p in counter.most_common()]
    # plt.bar(x, y)
    # plt.show()

    # 区间分布直方图

    # x = [random.randint(0, 100) for _ in range(100)]
    # plt.hist(x, rwidth=0.8, density=True)
    # plt.show()

    # x = [random.randint(50, 100) for _ in range(100)]
    # y = [random.randint(60, 80) for _ in range(100)]
    # plt.boxplot([x, y])
    # plt.show()
    n = 1
    x = []
    y = []
